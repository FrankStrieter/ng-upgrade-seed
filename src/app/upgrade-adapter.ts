import { UpgradeAdapter, UpgradeAdapterRef } from '@angular/upgrade';
import { UpgradeModule } from '@angular/upgrade/static';
import { forwardRef } from '@angular/core'
import { AppModule } from './app.module'

//export const UPGRADE_ADAPTER = new UpgradeAdapter(forwardRef(() => AppModule));

// export const BookGridComponent = UPGRADE_ADAPTER.upgradeNg1Component('bookGridComponent');
// export const BookGridElementComponent = UPGRADE_ADAPTER.upgradeNg1Component('bookGridElementComponent');
